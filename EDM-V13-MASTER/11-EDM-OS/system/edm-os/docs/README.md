# EDM OS — docs

- **../EDM-OS-Technical-Blueprint.pdf** — the full architecture & design package.
- **ui-prototype/dashboard.html** — open in any browser to see the Executive dashboard
  design (self-contained; no build step). The production version is the Next.js app in
  `apps/web`.
